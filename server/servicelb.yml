apiVersion: v1
kind: Service
metadata:
  name: backendlb
  labels : 
    app : backendlb
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5010
      targetPort: 5010
  selector:
    app: server
